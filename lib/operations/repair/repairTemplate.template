import {TransformationOperation} from "../TransformationOperation";
import {Bindings} from "@incremunica/incremental-types";

export class _ extends TransformationOperation {
  queryStr = `

`;
  operationName = "";

  protected _transform(bindings: Bindings) {

  }
}
